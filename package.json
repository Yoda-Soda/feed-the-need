{
  "name": "heroku-cra-node",
  "version": "3.0.0",
  "description": "How to use create-react-app with a custom Node API on Heroku",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start": "node server/index.js",
    "dbUpdate": "node -r dotenv/config tools/region-parser.js",
    "build": "cd server && npm install && cd ../client/ && npm install && npm run build && cp -r  build ../server/ui"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
